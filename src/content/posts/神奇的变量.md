
1️⃣ 知识目标🔦

- 复习C++的基本编程框架。🌐
- 学习变量的定义和使用。🖥️
- 学习使用算术运算符和赋值运算符(=)。
- 认识输入输出语句。🔐

2️⃣ 知识总结🔍：

### 一.变量的定义和使用

**1.什么是变量**

> 变量是存储信息的容器


**2.声明变量**![[屏幕截图 2024-05-14 111404.png]]
```Cpp
  int n = 1;
```

> int表示数据类型为整型，n为变量名，（=）等号表示赋值，值为1

**3.变量的命名规则**

> 可以给变量取名为你喜欢的任何名字。名字长短由你来定，里面可以有字母、数字、下划线（_）。不过对于变量名还有几条规则：

- 区分大小写，意思是大写和小写是不同的， 所以 teacher、Teacher、TEACHER 是三个不同的名字；
- 变量名不能以数字开头，所以 4name 不能作为变量名；
- 变量名不能包含空格；
- 变量名不能和关键字重名；

### 二、输入输出语句

输入语句：cin

```Cpp
cin >> n;
```

- _cin语句把空格字符和回车换行符作为分隔符，不输入给变量；_
- _cin语句忽略多余的输入数据_
- _仔细分析cin语句中变量的类型，按照相应的格式输入，否则容易出错。_

输出语句：cout

```Cpp
cout << n << endl;    //n是变量名，endl表示换行
```

3️⃣C++中的算术运算✒️：

**1.算术运算符**

**+**（加法） **、-**（减法） **、***（乘法） **、/**（除法） **、%**（取余）

ps:**整形变量做除法运算时，结果只取整数部分**

![[屏幕截图 2024-05-14 113021 1.png]]

**2. 取余运算**

> 一个整数对 10 取余，得到的是该数个位

![[屏幕截图 2024-05-14 113422.png]]
4️⃣编写C++程序💻：

**1. 变量的使用**

```Cpp
#include <iostream> //声明头文件
using namespace std; //使用标准名空间
int main() { //主函数是程序的入口
  int n = 1;
  cout << n << endl;
  return 0; //函数返回值为 0
}
```

**2. 算术运算**

```Cpp
#include<iostream>
using namespace std;
int main(){
  int c1 = 2,c2 = 1;
  cout << c1+c2 << endl;   //加法
  cout << c1-c2 << endl;   //减法
  cout << c1*c2 << endl;   //乘法
  cout << c1/c2 << endl;   //除法
  cout << c1%c2 << endl;   //取余
  return 0;
}
```